# Docker 用戶權限設置
# 解決容器內權限問題的環境變數

# 方法1: 自動獲取當前用戶 ID (Linux/macOS)
# 在運行 docker-compose 前執行:
# export USER_ID=$(id -u) && export GROUP_ID=$(id -g)

# 方法2: 手動設置用戶 ID
# USER_ID=1000
# GROUP_ID=1000

# 方法3: 使用 root 用戶（不推薦用於生產環境）
# 如需使用 root，請在 docker-compose.yml 中註釋掉 user 設置

# Docker 相關配置
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# 權限檢查設置
CHECK_PERMISSIONS=true
AUTO_FIX_PERMISSIONS=true

# 瀏覽器設置
BROWSER_SHARED_MEMORY_SIZE=2g
BROWSER_SECURITY_LEVEL=relaxed