# AoaCloud 自動打卡系統環境變數設定範例
# 複製此文件為 .env 並修改相關設定值

# ===== 登入憑證設定 (必要) =====
# 震旦HR系統登入憑證
COMPANY_ID=YOUR_COMPANY_ID          # 公司代號
USER_ID=YOUR_USER_ID                # 使用者帳號
PASSWORD=YOUR_PASSWORD              # 登入密碼

# ===== 打卡排程設定 (必要) =====
# 自動打卡時間設定
CLOCK_IN_TIME=09:00                 # 上班打卡時間 (HH:MM 格式)
CLOCK_OUT_TIME=18:00                # 下班打卡時間 (HH:MM 格式)
SCHEDULE_ENABLED=true               # 是否啟用自動排程 (true/false)
WEEKDAYS_ONLY=true                  # 僅工作日執行 (true/false)

# ===== 排程器狀態訊息設定 (選用) =====
STATUS_MESSAGE_INTERVAL=300         # 定期確認訊息間隔時間 (秒) 預設:300秒(5分鐘)

# ===== GPS 定位設定 (必要) =====
# 震旦行總部地址範例
GPS_LATITUDE=25.1             # GPS 緯度
GPS_LONGITUDE=121.5           # GPS 經度
GPS_ADDRESS=台北市   # 地址描述

# ===== 應用程式設定 (必要) =====
DEBUG=false                         # 除錯模式 (true/false)
HEADLESS=true                       # 無頭瀏覽器模式 (true/false)

# ===== Webhook 通知設定 (選用) =====
# Webhook 通知功能，支援多種平台
WEBHOOK_ENABLED=false                           # 是否啟用 webhook 通知 (true/false)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/1385585461002506403/LTC5KFV0wcVc6fEqonQXjTyIMxAuXifhl2eltjzMG_BZpFpds07_x2Wp95arprwVGn8r                            # Discord webhook URL (選用)

# Webhook 通知設定
WEBHOOK_NOTIFY_SUCCESS=true                     # 成功打卡時發送通知 (true/false)
WEBHOOK_NOTIFY_FAILURE=true                     # 打卡失敗時發送通知 (true/false)
WEBHOOK_NOTIFY_SCHEDULER=true                   # 排程器事件時發送通知 (true/false)
WEBHOOK_NOTIFY_ERRORS=true                      # 系統錯誤時發送通知 (true/false)

# Webhook 進階設定
WEBHOOK_TIMEOUT_SECONDS=30                      # 請求超時時間 (秒)
WEBHOOK_RETRY_ATTEMPTS=3                        # 重試次數
WEBHOOK_RATE_LIMIT_DELAY=1.0                   # 速率限制延遲 (秒)

# ===== Discord Webhook 設定說明 =====
# 1. 前往 Discord 伺服器設定 > 整合 > Webhook
# 2. 建立新的 Webhook 並複製 URL
# 3. 將 URL 貼到 DISCORD_WEBHOOK_URL 設定中
# 範例: https://discord.com/api/webhooks/123456789/abcdefghijk

# ===== 使用說明 =====
# 1. 複製此文件為 .env: cp .env.example .env
# 2. 修改上方的所有設定值（所有環境變數都是必要的）
# 3. 確保 .env 文件不會被提交到版本控制系統

# ===== 布林值設定說明 =====
# 支援的 true 值: true, 1, yes, on
# 支援的 false 值: false, 0, no, off